# P0.5.1 — Active Draft Editor Integration

## Status: PASS

## Overview

P0.5.1 integrates the "Active Draft" concept (introduced in P0.4) into the content editing workflow. The core principle is:

> **Manual edits create new `MANUAL_EDIT` drafts instead of overwriting old versions.**

This preserves complete editing history while giving users a seamless inline editing experience.

## Goals

1. **Editor binding**: Editor binds to the active draft (not `drafts[0]`)
2. **Version preservation**: Save creates a new draft; old versions untouched
3. **Dirty state protection**: Dirty editor silently blocks cross-tab overwrites
4. **Atomic transactions**: Version creation + activeDraftId update in single transaction
5. **Cross-tab awareness**: Broadcast after save; remote refresh for clean editors
6. **User experience**: Inline title/content editing with visible version context

## Architecture

### Component Flow

```
Project/[id]/page.tsx
  ↓
 Reads topic.activeDraft + allDrafts
  ↓
 <DraftEditor activeDraft={...} allDrafts={...} topicId={...} />
  ↓
 useActiveDraftSync (cross-tab sync)
  ↓ onChange → isDirty=true
  ↓ onSave → saveManualDraftEdit(sourceDraftId, content, title)
  ↓ success → broadcastActiveDraftChange → router.refresh()
```

### Data Flow

```
User Edit → isDirty=true → Click Save
  ↓
saveManualDraftEdit(sourceDraftId, content, title)
  ↓
createManualEditDraft({ sourceDraftId, content, title, userId })
  ↓
prisma.$transaction {
  1. Re-read max version (inside tx, for freshness)
  2. Create new Draft (version++, parentDraftId=sourceDraft.id)
  3. Update Topic.activeDraftId = newDraft.id
}
  ↓
revalidatePath('/projects/(app)')
broadcastActiveDraftChange(topicId, newDraft.id, sourceId)
router.refresh() → props re-render with new active draft
```

## Files

### Modified

| File | Change |
|------|--------|
| `src/lib/repositories/topic-repository.ts` | Added `createManualEditDraft()` method with transaction + retry |
| `src/lib/services/server-actions.ts` | Added `saveManualDraftEdit()` server action with error mapping |
| `src/app/(app)/projects/[id]/page.tsx` | Integrated `<DraftEditor>` component |

### Created

| File | Purpose |
|------|---------|
| `src/components/projects/draft-editor.tsx` | React editor component with dirty state, cross-tab conflict detection |
| `src/lib/repositories/__tests__/manual-edit-draft.test.ts` | 13 repository tests (ownership, transaction, retry, conflict) |
| `src/lib/services/__tests__/save-manual-draft-edit.test.ts` | 12 server action tests (validation, error mapping, revalidate) |
| `src/components/projects/__tests__/draft-editor-behavior.test.tsx` | 13 React behavior tests (UI interactions, save flow, conflict UI) |

## Key Design Decisions

### 1. Manual Edit = New Draft (Not Update)

The existing `Draft` model already tracks `version`, `parentDraftId`, `changeType`, `changeReason`. We use `changeType='MANUAL_EDIT'` to distinguish manual edits from RESTORE, AI_GENERATE, etc.

### 2. Monotonic Versioning with Retry

Concurrent edits could produce duplicate versions. Solution:
- Re-read `max(version)` inside the transaction (not before)
- Retry up to 3 times on unique constraint violation (P2002)
- Fallback error: `DRAFT_VERSION_CONFLICT`

### 3. Dirty State Protection

Prevents cross-tab data loss:
- **Clean editor** (not dirty): remote active draft change → `router.refresh()` silently
- **Dirty editor**: remote change → shows amber conflict warning banner, user must choose to refresh or dismiss

### 4. Broadcast After Save

The save flow:
1. Create new draft (server action)
2. Update refs to new baseline (`initialTitleRef`, `initialContentRef`)
3. Set `isDirty=false`
4. `broadcastActiveDraftChange()` — notify other tabs
5. `router.refresh()` — re-render with new active draft

### 5. Error Mapping

Database errors mapped to user-friendly Chinese messages:

| Error Code | Message |
|-----------|---------|
| `SOURCE_DRAFT_NOT_FOUND` | 源草稿不存在，请刷新页面重试 |
| `TOPIC_NOT_FOUND` | 主题不存在，请刷新页面重试 |
| `OWNERSHIP_DENIED` | 无权操作此草稿 |
| `DRAFT_VERSION_CONFLICT` | 版本冲突，请刷新后重试 |
| (other) | 保存失败，请稍后重试 |

### 6. Parent Version Display

The editor shows `来源：vN` when the current active draft has a `parentDraftId`. This is computed via `useMemo` from the `allDrafts` prop.

## Test Results

### P0.5.1 Tests (38 total)

- `manual-edit-draft.test.ts`: 13/13 PASS
  - Input validation, ownership check, transaction atomicity, retry on conflict, version increment, activeDraftId update
- `save-manual-draft-edit.test.ts`: 12/12 PASS
  - Input validation, error mapping, revalidate path, success response
- `draft-editor-behavior.test.tsx`: 13/13 PASS
  - Editor binding, dirty state tracking, save button enable/disable, save API call params, success flow, failure content preservation, remote conflict protection

### Regression

- `src/components/projects/__tests__/`: **330/330 PASS**
  - All P0.4.x tests (P0.4.3, P0.4.6, P0.4.8, P0.4.8.1, P0.4.9, P0.4.9.1) still pass
  - No pre-existing failures introduced

### Typecheck

- Pre-existing baseline failures in `knowledge/semantic` modules (unrelated)
- No new errors introduced by P0.5.1

### Lint

- All P0.5.1 files lint-clean (0 errors, 0 warnings)
- Pre-existing baseline warnings in `topic-repository.ts` `{}` types (unrelated)

## Acceptance Criteria

| Criteria | Status |
|----------|--------|
| Editor binds to active draft | ✅ |
| Save creates new MANUAL_EDIT draft | ✅ |
| Old versions preserved | ✅ |
| Atomic transaction (version + activeDraftId) | ✅ |
| Retry on concurrent edit conflict | ✅ |
| Dirty state prevents silent overwrite | ✅ |
| Clean editor refreshes silently on remote change | ✅ |
| Broadcast after successful save | ✅ |
| All 330 regression tests pass | ✅ |
| 38 new P0.5.1 tests pass | ✅ |

## P0.5.1 FINAL CHECK

```
Status: PASS

1. Active Draft Editor        ✅ PASS
2. Manual Edit = New Draft    ✅ PASS
3. Transaction Atomicity      ✅ PASS
4. Cross-Tab Protection       ✅ PASS
5. Regression (330/330)       ✅ PASS
6. New Tests (38/38)          ✅ PASS
7. Typecheck                  ✅ PASS (pre-existing baseline only)
8. Lint                       ✅ PASS (all new files clean)

Final: P0.5.1 PASS
```
