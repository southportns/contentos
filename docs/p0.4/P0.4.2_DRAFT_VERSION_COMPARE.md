# P0.4.2 — Draft Version Compare

## 1. Objective

P0.4.1 solved "What content versions do I have?". This phase solves:

**"What actually changed between two versions?"**

Enable users to:
- Select two versions to compare
- View line-level diff (added/removed/unchanged)
- View evaluation data comparison for both versions
- Read-only operations, no data modification

## 2. Scope

### In Scope
- Version Compare Mode — switch to compare mode in version history
- Version A/B Selector — two dropdown selectors for version selection
- Line-based Diff Algorithm — pure LCS function
- Diff Display — visual diff display (+/- markers)
- Version Metadata Comparison — evaluation/strategy/humanization comparison
- Same Version Protection — prevent comparing same version with itself
- Swap Button — quickly swap A/B versions
- Read-only — purely display layer
- **P0.4.1 Detail Mode preserved** — Evaluation / Strategy Evaluation / Humanization detail view

### Out of Scope
- Version restore, edit, delete
- AI judgment / automatic feedback
- Database changes
- Character-level diff
- Version export

## 3. Out of Scope (Hard Limits)

- **No Prisma schema changes** — no new tables/columns
- **No database changes** — no migrations
- **No Workflow modifications** — Writing/Evaluation/Refine/Humanization/Strategy Approval unchanged
- **No deletion of existing features** — P0.4.1 Detail Mode preserved

## 4. Architecture

```
DraftVersionHistory (client)
  ├─ Detail Mode (default) — P0.4.1 fully preserved
  │   ├─ Version List (left sidebar)
  │   └─ Selected Version Detail (right panel)
  │       ├─ Title / Metadata / Content
  │       ├─ Evaluation (overall/emotional/clarity/novelty/readability/platform)
  │       ├─ Strategy Evaluation (overall/grade/platform/consistency)
  │       └─ Humanization (adopted/not adopted)
  └─ Compare Mode (P0.4.2 new)
      ├─ DraftVersionCompareSelector (A/B selection + Swap)
      ├─ Version Metadata Cards (A vs B)
      └─ DraftVersionDiff (LCS line diff)
```

### Component Hooks Safety

```
DraftVersionCompare          ← conditional return (if drafts.length < 2)
    ↓
DraftVersionCompareContent   ← ALL hooks called unconditionally at top level
    ├─ useState (versionA, versionB)
    ├─ useMemo (draftA, draftB, diffLines)
    └─ helper functions (getDefaultCompareVersions, canCompareVersions, swapCompareVersions)
```

## 5. Acceptance Matrix

| Category | Item | Status |
|----------|------|--------|
| Detail Mode | Evaluation display (6 dimensions) | PASS |
| Detail Mode | Strategy Evaluation display (4 fields) | PASS |
| Detail Mode | Humanization status (已采用/未采用) | PASS |
| Detail Mode | Empty state (暂无分析数据) | PASS |
| Compare Entry | 2+ drafts shows button | PASS |
| Compare Entry | 0 or 1 draft: no button | PASS |
| Version A/B | Default A = latest (drafts[0]) | PASS |
| Version B | Default B = previous (drafts[1]) | PASS |
| Swap | swapCompareVersions exchanges A/B correctly | PASS |
| Same Version | canCompareVersions: A===B returns false | PASS |
| Diff Display | Line-level LCS diff with +/- markers | PASS |
| Metadata Cards | A/B cards show evaluation/strategy/humanization | PASS |
| Read-only | No create/update/delete operations | PASS |
| Schema | Prisma schema unchanged | PASS |

## 6. Files Changed

### New Files
- `src/components/projects/draft-version-compare.tsx` (7948 bytes) — Compare panel + DraftVersionCompareContent
- `src/components/projects/draft-version-compare-selector.tsx` (3209 bytes) — A/B selectors + swap button
- `src/components/projects/draft-version-diff.tsx` (1256 bytes) — Diff line renderer
- `src/components/projects/__tests__/draft-version-compare.test.ts` — 22 tests
- `src/components/projects/__tests__/draft-version-utils.test.ts` — 22 tests
- `src/components/projects/__tests__/detail-mode-regression.test.ts` — 16 regression tests
- `docs/p0.4/P0.4.2_DRAFT_VERSION_COMPARE.md` — This document

### Modified Files
- `src/components/projects/draft-version-utils.ts` (5752 bytes) — Added compareDraftVersions, DiffLine, getDefaultCompareVersions, canCompareVersions, swapCompareVersions
- `src/components/projects/draft-version-history.tsx` (9577 bytes) — Added compare mode toggle (P0.4.2); Restored Detail Mode Evaluation/Strategy/Humanization sections (P0.4.1 regression fix)

### Schema Changes
**None** — Prisma schema not modified, no new tables/columns.

## 7. Verification

### Test Commands
```bash
npx vitest run src/components/projects/__tests__/
npm test
npm run typecheck
npm run lint
npm run build
```

### Test Results

#### P0.4.2 Tests (src/components/projects/__tests__/)
- **detail-mode-regression.test.ts**: 16/16 PASS
- **draft-version-history.test.ts**: 26/26 PASS
- **draft-version-utils.test.ts**: 22/22 PASS
- **draft-version-compare.test.ts**: 22/22 PASS
- **Total**: 86/86 PASS

#### Full Test Suite (npm test)
- **Total Tests**: 685 passed / 33 failed (718 total)
- **Test Files**: 37 passed / 13 failed (50 total)
- **P0.4.2 Tests**: 86/86 PASS (all in src/components/projects/__tests__/)
- **P0.4.1 Tests**: 26/26 PASS (draft-version-history.test.ts)

#### Failures Classification — ALL PRE-EXISTING BASELINE
| Failures | Root Cause | Module |
|----------|-----------|--------|
| 16 | SemanticRetriever is not a constructor | knowledge/semantic/ |
| 9 | SemanticRetriever is not a constructor | knowledge/production-retrieval |
| 5 | Cannot read properties of undefined | knowledge/retrieval-calibration |
| 3 | computeRefineSignature assertion mismatch | create/manual-content-expiration |

**None of the 33 failures are caused by P0.4.2 changes.** All are pre-existing in `knowledge/semantic`, `knowledge/production-retrieval`, `knowledge/retrieval-calibration`, and `create/manual-content-expiration` modules.

#### Zero-byte files
**NONE** — All P0.4.2 files have valid byte sizes (1256–9577 bytes).

#### Schema
**UNCHANGED** — No Prisma schema modification, no new migrations.

### TypeScript
- **Status**: FAIL_WITH_BASELINE_ISSUES
- **P0.4.2 files**: ZERO errors
- **Pre-existing errors**: 10 in scripts/ and knowledge/semantic/ (SemanticIndex, SemanticRetriever exports missing — corrupted from prior push incident)

### Lint
- **Status**: FAIL_WITH_BASELINE_ISSUES
- **P0.4.2 files**: ZERO errors, ZERO warnings
- **Pre-existing issues**: 130 problems (65 errors, 65 warnings) across scripts/, knowledge/semantic/persistence/, and other pre-existing modules

### Build
- **Status**: FAIL_WITH_BASELINE_ISSUES
- **Pre-existing errors**: 3 export errors in knowledge/semantic/ (SemanticIndex, SemanticRetriever, knowledgeStore — caused by corrupted files from prior push incident)

## 8. Hardening Fixes

### Fix 1: Restore P0.4.1 Detail Mode (Evaluation/Strategy/Humanization)
- Restored Evaluation display (综合/情感冲击/逻辑清晰/新颖度/可读性/平台适配) using all 6 score fields
- Restored Strategy Evaluation display (综合/等级/平台/一致性) with all 4 fields
- Restored Humanization status (已采用/未采用) based on `adopted` boolean
- Added "暂无分析数据" fallback when no analysis exists

### Fix 2: Fix React Hooks Ordering
- **Before**: `if (drafts.length < 2) return ...` BEFORE `useState()` calls → Violation
- **After**: Split into `DraftVersionCompare` (conditional return) + `DraftVersionCompareContent` (all hooks at top level)
- Hooks order now stable for every component instance

### Fix 3: Remove Invalid Assertions
- Removed: `expect(3 === 3).toBe(true)`, hand-rolled swap (`const t = a; a = b; b = t`)
- Replaced with real function tests: `swapCompareVersions()`, `canCompareVersions()`, `getDefaultCompareVersions()`

### Fix 4: Extract Pure Helpers
- Added to `draft-version-utils.ts`:
  - `getDefaultCompareVersions(drafts)` → `{versionA, versionB}` or nulls
  - `canCompareVersions(a, b)` → boolean
  - `swapCompareVersions(a, b)` → swapped positions
- Both component and tests use the SAME production functions

### Fix 5: Add Regression Coverage
- Created `detail-mode-regression.test.ts` with 16 static regression tests
- Verifies both P0.4.1 Detail Mode fields AND P0.4.2 Compare Mode entry points coexist
- Prevents future regressions when modifying either mode

## 9. Limitations

1. **Read-only** — Compare is pure frontend display, no persistence
2. **Line-level only** — No character-level diff
3. **No version restore** — Cannot revert to an older version from compare view
4. **Max 2 versions** — Compare is A/B only (no multi-version merge view)
5. **Same-version protection** — Selecting same A/B shows message, not diff

## 10. Acceptance Verification History

| Date | Commit | Tests | Notes |
|------|--------|-------|-------|
| 2026-09-21 | bbccd37 | 86/86 P0.4.2 PASS, 685/718 total PASS | Final verification: P0.4.1 + P0.4.2 complete |

## 11. Final Verdict

**PASS**

Reason:
- P0.4.2 all 86 tests pass (22 utils + 22 compare + 26 history + 16 regression)
- P0.4.1 Detail Mode fully restored (Evaluation 6 dimensions, Strategy 4 fields, Humanization adopted/not)
- React Hooks ordering correct (DraftVersionCompare → DraftVersionCompareContent split)
- Prisma schema unchanged (22235 bytes, 4 migrations, no new migrations)
- P0.4.2 files Lint zero errors
- Typecheck/Build failures are 100% PRE-EXISTING BASELINE (knowledge/semantic corrupted files from prior push incident)
- No existing business logic modified
- Regression coverage (16 tests) prevents future breakage
- All file sizes valid (1256–9577 bytes), zero-byte files: NONE
